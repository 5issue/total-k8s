apiVersion: apps/v1
kind: Deployment
metadata:
  name: product-service
  namespace: backend
  labels:
    app: product-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: product-service
  template:
    metadata:
      labels:
        app: product-service
    spec:
      nodeSelector:
        karpenter.sh/capacity-type: spot
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: "topology.kubernetes.io/zone"
          whenUnsatisfiable: ScheduleAnyway
          labelSelector:
            matchLabels:
              app: product-service
      serviceAccountName: backend-common-sa
      containers:
        - name: product-service
          image: 596601390909.dkr.ecr.ap-northeast-2.amazonaws.com/kurly-product:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
          env:
            - name: AUTH_JWKS_URI
              value: "http://auth-service.backend.svc.cluster.local:8080/.well-known/jwks.json"
            - name: SPRING_RABBITMQ_HOST
              value: "rabbitmq.messaging.svc.cluster.local"
            - name: SPRING_RABBITMQ_PORT
              value: "5671"
            - name: SPRING_RABBITMQ_SSL_ENABLED
              value: "true"
            - name: RABBITMQ_SSL_CERT_PATH
              value: "/etc/ssl/certs/rabbitmq/ca.crt"
            - name: DB_HOST
              value: "shared-pg-rw.backend.svc.cluster.local"
            - name: DB_PORT
              value: "5432"
            - name: DB_NAME
              value: "product_db"
            - name: DB_USERNAME
              value: "product_user"
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: shared-pg-product-service-credentials
                  key: password
          volumeMounts:
            - name: rabbitmq-ca
              mountPath: /etc/ssl/certs/rabbitmq
              readOnly: true
          resources:
            requests:
              cpu: "250m"
              memory: "512Mi"
            limits:
              cpu: "500m"
              memory: "1Gi"
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8080
            initialDelaySeconds: 15
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 15
      volumes:
        - name: rabbitmq-ca
          secret:
            secretName: rabbitmq-ca
            items:
              - key: ca.crt
                path: ca.crt
---
apiVersion: v1
kind: Service
metadata:
  name: product-service
  namespace: backend
  labels:
    app: product-service
spec:
  type: ClusterIP
  selector:
    app: product-service
  ports:
    - name: http
      port: 8080
      targetPort: 8080