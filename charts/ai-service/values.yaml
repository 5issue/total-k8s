serviceName: kurly-ai-serving
replicaCount: 1

image:
  registry: 596601390909.dkr.ecr.ap-northeast-2.amazonaws.com
  repository: kurly-ai-assistant
  tag: "latest"
  pullPolicy: Always

serverPort: 8000

# CNPG 연결 설정
database:
  host: shared-pg-rw.backend.svc.cluster.local
  port: 5432
  name: ai_db
  username: ai_user
  secretName: shared-pg-ai-service-credentials
  passwordKey: password

health:
  livenessDelay: 20
  readinessDelay: 15
# 테스트용 권장 리소스 설정
resources:
  requests:
    cpu: 50m
    memory: 256Mi
  limits:
    cpu: 250m
    memory: 512Mi

networkPolicy:
  enabled: true
  allowFromNamespaces:
    - backend   # 백엔드 마이크로서비스에서 AI 호출 허용
    - dev       # dev 네임스페이스 내부 파드 통신 허용
  # allowFromCidrs는 아예 적지 않거나 비워둠 (ALB/외부 Ingress 원천 차단)
  allowFromCidrs:
    - "10.0.0.0/16"   # ALB가 위치한 VPC 서브넷 대역

serviceAccount:
  create: true
  name: ai-sa
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::596601390909:role/ai-service-irsa
# # LLM 연동 설정 (OpenRouter)
# llm:
#   provider: openrouter
#   baseUrl: https://openrouter.ai/api/v1
#   model: "TODO-ai팀-모델명"        # TODO: AI팀 환경변수 답변 대기
#   apiKeySecret: ai-llm-secret
